
Classification
def Preprocessing():
    read csv('c')
    binning
    handling nulls (new)
    enconding Sym
    encoding features
    scaling
    feature selection  -> list names final features df[]

def read_pickles():


def run_models():
    y_pred = predict(x)
    accuracy = y y-pred
    print

Regression
def Preprocessing():
    read csv('r')
    binning
    handling nulls (new)
    enconding Sym  
    encoding features different nominal -> doesn't handle unknown categories
    scaling 
    feature selection  -> list names final features df[] -> different features

def run_models():
    y_pred = predict(x)
    r2, mse
    print